# generation runs (large, stochastic intermediates — the shipped artifacts are the record)
runs/
*.sg-rwlock
*_32/
run*/
*_cut.png          # matted intermediate: reproducible from sheet.png + the key

# python
__pycache__/
*.pyc
.venv/
venv/

# os
.DS_Store
Thumbs.db
